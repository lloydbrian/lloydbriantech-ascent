# <<PROJECT_TITLE>> — environment variables
# Copy to .env and fill in values. Empty defaults = the schema.
# See ENV-DISCIPLINE: never use REPLACE_ME or fake placeholder values.

<<ENV_PREFIX>>_PORT=
<<ENV_PREFIX>>_HOST=
<<ENV_PREFIX>>_NODE_ENV=
<<ENV_PREFIX>>_LOG_LEVEL=
<<ENV_PREFIX>>_DB_URL=
