# Working memory — <<PROJECT_TITLE>>

Accumulating decisions that persist across sessions. This file is git-tracked and append-only. Each entry records a decision, adopted pattern, or rejected pattern with a date.

See `session-protocol.md` in the framework's reference modules for how Claude reads this file on session start.

---

## Decisions

## Patterns adopted

## Patterns rejected
